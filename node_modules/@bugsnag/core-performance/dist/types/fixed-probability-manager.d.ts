import type { ReadWriteSampler } from './sampler';
declare class FixedProbabilityManager {
    static create(sampler: ReadWriteSampler, samplingProbability: number): Promise<FixedProbabilityManager>;
    private readonly sampler;
    private readonly samplingProbability;
    private constructor();
    setProbability(newProbability: number): Promise<void>;
    ensureFreshProbability(): Promise<void>;
}
export default FixedProbabilityManager;
//# sourceMappingURL=fixed-probability-manager.d.ts.map