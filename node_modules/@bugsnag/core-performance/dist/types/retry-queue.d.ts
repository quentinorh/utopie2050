import type { Delivery, TracePayload } from './delivery';
export interface RetryQueue {
    add: (payload: TracePayload, time: number) => void;
    flush: () => Promise<void>;
}
export type RetryQueueFactory = (delivery: Delivery, retryQueueMaxSize: number) => RetryQueue;
export declare class InMemoryQueue implements RetryQueue {
    private delivery;
    private retryQueueMaxSize;
    private requestQueue;
    private payloads;
    constructor(delivery: Delivery, retryQueueMaxSize: number);
    add(payload: TracePayload, time: number): void;
    flush(): Promise<void>;
}
//# sourceMappingURL=retry-queue.d.ts.map