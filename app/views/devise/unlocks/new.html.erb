<% content_for :title, "SP 2050 - Déverrouiller mon compte" %>
<% content_for :description, "Reçois un nouveau lien pour déverrouiller ton compte SP 2050." %>
<% content_for :meta_robots, "noindex, nofollow" %>
<% content_for :navbar_transparent, true %>

<div class="auth-page" data-controller="auth-background">
  <!-- Animated SVG pattern background -->
  <div class="auth-bg" data-auth-background-target="svg"></div>
  <div class="auth-overlay"></div>

  <!-- Form -->
  <div class="auth-form-container">
    <div class="auth-form">
      <h2 class="auth-form__title">Debloquer mon compte</h2>
      <p class="auth-form__subtitle">On te renvoie les instructions par email</p>

      <%= simple_form_for(resource, as: resource_name, url: unlock_path(resource_name), html: { method: :post }) do |f| %>
        <%= f.error_notification %>
        <%= f.full_error :unlock_token, class: "text-red-400 text-sm mt-2 text-center" %>

        <div class="auth-form__fields">
          <%= f.input :email,
                      label: "Adresse e-mail",
                      required: true,
                      autofocus: true,
                      input_html: { autocomplete: "email", class: 'auth-input', placeholder: "ton@email.com" },
                      label_html: { class: "auth-label" } %>
        </div>

        <div class="auth-form__actions">
          <%= f.button :submit, "Renvoyer les instructions", class: "auth-btn-primary", data: { form_type: "other" } %>
        </div>
      <% end %>

      <%= render "devise/shared/links" %>
    </div>
  </div>
</div>
